
{
//  System.out.println("start" + name);

     $proceed($$);

    try {

    // 防止递归调用，name是Logger的属性
  if("trace".equals(name)){
return;
}

  Logger traceLog = LoggerFactory.getLogger("trace");
  if(traceLog == null){
    return;
  }
                traceLog.info($4,$5);
      //  System.out.println("end" + name);


    } finally {
    }
}