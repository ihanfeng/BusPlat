
spring:
  jmx:
    default-domain: ${spring.application.name}

server:
  contextPath: /${spring.application.name}
#  port: 8080


endpoints:
  jmx:
    unique-names: true

## acutor 设置
management:
  port: 18081
  address: 127.0.0.1
  security:
    enabled: false
    role: SUPERUSER

security:
  basic:
    enabled: false
  user:
    name: admin
    password: admin